import { laterWeeks } from "./full-curriculum-later-weeks.mjs";

const official = "THEO_KE_HOACH_CO";
const extended = "KIEN_THUC_MO_RONG";

const base = {
  estimatedMinutes: 210,
  quizPassScore: 75,
  objectives: [
    "Giải thích concept bằng analogy và technical language",
    "Tự chạy lab và phân biệt expected/actual",
    "Debug failure bằng evidence protocol",
  ],
  cleanup: "khôi phục đúng file/config đã sao lưu trong lab và xác nhận use case cũ vẫn pass; không xóa volume",
  regression: "chạy lại caller, healthcheck và một use case lân cận bị ảnh hưởng bởi change",
};

function mission(value) {
  return { ...base, ...value };
}

export const weekCatalog = [
  {
    week: 2,
    previousMissionId: "w1-m7-docker-compose-baseline",
    missions: [
      mission({
        id: "w2-m1-docker-image-container",
        slug: "docker-image-container",
        title: "Docker Image, Container & Dockerfile",
        category: "Docker",
        skillId: "docker",
        targetLevel: "L4",
        sourceType: official,
        competency: "Dockerfile, image, container, layers, build context, build/run/exec/inspect/logs",
        keywords: ["dockerfile", "image", "container", "layers", "build context", "inspect", "logs"],
        analogy: "Image giống khuôn bánh đã đóng gói; container là chiếc bánh đang được nướng từ khuôn đó. Dockerfile là công thức, còn build context là túi nguyên liệu được gửi cho thợ nướng. Gửi cả kho vào túi làm build chậm và dễ lộ file không cần thiết.",
        easy: "Docker đọc Dockerfile theo từng bước để tạo image bất biến. Mỗi lần run tạo một container có process và filesystem riêng. Sửa file bên trong container không tự sửa Dockerfile hay image, nên fix đúng phải quay về source/config rồi rebuild.",
        technical: "An image is a content-addressed, layered filesystem plus runtime metadata. A container is an isolated runtime instance of an image. Docker sends the selected build context to the builder, executes Dockerfile instructions, reuses cacheable layers, then starts the configured process through CMD/ENTRYPOINT.",
        why: "SV1 phải build artifact tái lập được, xem layer/cache, vào container để thu evidence và phân biệt lỗi build với lỗi runtime.",
        subtopics: ["base image và pinned tag", "Dockerfile FROM/WORKDIR/COPY/RUN/CMD", "layer cache", ".dockerignore và build context", "docker build/run/exec/inspect/logs"],
        command: "docker build -t dxlab-api:0.2 ./backend\ndocker run --rm --name dxlab-api-test -p 8000:8000 dxlab-api:0.2\ndocker exec dxlab-api-test sh -lc 'pwd && ps'\ndocker inspect dxlab-api-test\ndocker logs --tail=100 dxlab-api-test",
        where: "Host terminal tại repository root; đường dẫn ./backend phải tồn tại trong project lab thật.",
        commandWhy: "Build image có tag, chạy instance tạm, rồi kiểm process/metadata/log thay vì đoán.",
        expected: "Build kết thúc với image tag dxlab-api:0.2; container có process chính; inspect cho thấy image/port; logs có startup line liên quan.",
        commandFailure: "Build context sai, COPY không tìm thấy file, base tag không tồn tại, process exit hoặc app chỉ bind localhost trong container.",
        guided: "Tạo Dockerfile nhỏ cho HTTP service mẫu, thêm .dockerignore loại .git/.env/node_modules, build hai lần và so sánh layer nào dùng cache. Run service, gọi health, xem inspect/log rồi dừng container tạm.",
        independent: "Từ một service khác, tự viết Dockerfile không copy secret, pin base tag, build và giải thích thứ tự COPY nào tối ưu cache mà vẫn đúng.",
        integration: "Build Backend image rồi chứng minh Portal/proxy gọi được published port; nếu source thuộc SV2, handoff Dockerfile/runtime evidence thay vì sửa business code.",
        failure: "Cố tình đặt COPY tới file ngoài build context hoặc đổi CMD sang port khác. Thu build/runtime evidence và xác định failure xảy ra trước hay sau khi container được tạo.",
        errors: ["Nghĩ image và container là một", "Dùng latest không kiểm soát", "Copy .env vào image", "Sửa trực tiếp container rồi coi là fix", "Gửi toàn repository làm context"],
        selfChecks: ["Vì sao rebuild mới thay đổi image?", "Layer cache bị invalid từ instruction nào?", "exec khác run thế nào?", "Build context có liên quan gì tới secret?"],
        evidence: ["terminal-output: build/run/inspect/log relevant lines", "config: Dockerfile + .dockerignore đã redacted", "explanation: image/layer/container causal chain"],
        requiredEvidence: ["terminal-output", "config", "explanation"],
        quizConcept: "Image là artifact layered; container là runtime instance được tạo từ image.",
        firstTest: "Đọc first relevant build/runtime error rồi đối chiếu Dockerfile, build context và process command.",
        shortPrompt: "Lệnh nào tạo image từ Dockerfile và build context?",
        shortAnswer: "docker build",
        shortExplanation: "docker build gửi context và thực thi Dockerfile để tạo image.",
        explainPrompt: "Giải thích Dockerfile → layer → image → container và vì sao sửa trong container không phải release fix.",
        passExplanation: "build context, layer cache và runtime process",
        returnBlock: "Dockerfile/build-context hoặc image/container mental model",
      }),
      mission({
        id: "w2-m2-compose-contracts",
        slug: "compose-contracts",
        title: "Compose Service Contracts",
        category: "Docker Compose",
        skillId: "compose",
        targetLevel: "L4",
        sourceType: official,
        competency: "Compose services, build/image, ports, environment, env_file, networks, volumes, depends_on, healthcheck, restart",
        keywords: ["compose", "services", "ports", "environment", "depends_on", "restart", "config"],
        analogy: "Compose giống sơ đồ sân khấu: mỗi diễn viên là service, cửa ra vào là port, phòng hậu trường là network, kho đạo cụ là volume và cue bắt đầu là dependency. Sơ đồ đúng cú pháp chưa chứng minh vở diễn chạy đúng.",
        easy: "Compose gom cách build/run nhiều container thành một contract. docker compose config cho biết contract sau khi biến môi trường được thay thế. depends_on điều khiển thứ tự/điều kiện khởi động, nhưng app vẫn cần retry vì dependency có thể mất sau startup.",
        technical: "A Compose model declares services and their image/build source, process environment, network attachments, published ports, persistent mounts, healthchecks, restart policy and dependency conditions. The resolved model is the source for runtime orchestration, not a substitute for functional tests.",
        why: "SV1 owner Compose contract và phải review mọi hostname/port/env/health/dependency ảnh hưởng integration.",
        subtopics: ["services và project name", "build vs image", "HOST:CONTAINER port mapping", "environment vs env_file", "networks/volumes", "depends_on service_healthy", "restart policy"],
        command: "docker compose config\ndocker compose build\ndocker compose up -d\ndocker compose ps\ndocker compose logs --tail=100 backend",
        where: "Host terminal tại directory chứa docker-compose.yml/compose.yaml và .env của lab.",
        commandWhy: "Validate resolved model trước, rồi build/start và quan sát đúng service.",
        expected: "config hiển thị service contract đã resolve; ps cho biết state/health/ports; logs cho startup hoặc first relevant error.",
        commandFailure: "Chạy sai directory, biến interpolation thiếu, port host bị dùng, service name typo hoặc healthcheck gọi tool không có trong image.",
        guided: "Đọc một Compose ba service; lập bảng service/build-or-image/port/env/network/volume/health/dependency rồi so với docker compose config. Start và chứng minh từng cột bằng ps/inspect/log.",
        independent: "Tự thêm restart: unless-stopped và health-based dependency cho Backend mà không đổi business code; giải thích giới hạn của restart loop.",
        integration: "Trace Browser → published port → Backend internal port → postgres service; ghi rõ URL bên ngoài và hostname bên trong.",
        failure: "Cố tình đổi container target port nhưng giữ host port. Quan sát config, ps, listen evidence và caller symptom; fix contract tối thiểu.",
        errors: ["Đọc YAML gốc nhưng không xem config đã resolve", "Nhầm host/container port", "Tin depends_on bảo đảm app luôn sẵn sàng", "Đặt secret thật trong Compose", "Restart che crash loop"],
        selfChecks: ["build khác image thế nào?", "ports 8000:8001 nghĩa gì?", "env_file có tự che secret khỏi inspect không?", "Vì sao app vẫn cần retry?"],
        evidence: ["config: resolved Compose excerpt không chứa secret", "terminal-output: ps/log/inspect evidence", "explanation: service contract và dependency limits"],
        requiredEvidence: ["config", "terminal-output", "explanation"],
        quizConcept: "Compose là declarative multi-service runtime contract; functional PASS vẫn cần health và use-case test.",
        firstTest: "Chạy docker compose config và so resolved service/port/env/network với expected contract.",
        shortPrompt: "Lệnh nào validate và render Compose model đã resolve?",
        shortAnswer: "docker compose config",
        shortExplanation: "config parse, interpolate và hiển thị model mà engine sẽ dùng.",
        explainPrompt: "Giải thích build/image, ports, depends_on, healthcheck và restart trong một Compose service.",
        passExplanation: "resolved Compose contract và running/healthy/functional",
        returnBlock: "Compose config, port mapping hoặc dependency contract",
      }),
      mission({
        id: "w2-m3-docker-network-dns",
        slug: "docker-network-dns",
        title: "Docker Network, Service DNS & localhost Trap",
        category: "Docker Network",
        skillId: "docker-network",
        targetLevel: "L4",
        sourceType: official,
        competency: "Bridge networks, service DNS, service name/hostname, localhost, internal and published ports",
        keywords: ["bridge", "dns", "service name", "hostname", "localhost", "internal port", "published port"],
        analogy: "Mỗi container giống một căn hộ có số nội bộ. localhost là gọi điện cho chính căn hộ đó; service name là danh bạ chung trong tòa nhà; published port là quầy lễ tân chuyển cuộc gọi từ bên ngoài.",
        easy: "Containers cùng Compose network thường resolve nhau bằng service name. Backend phải gọi postgres:5432, không phải localhost:5432. Browser trên host không dùng service name; nó gọi localhost hoặc domain qua published port/proxy.",
        technical: "A user-defined bridge network provides isolated L2/L3 connectivity and embedded DNS for service aliases. Network namespaces make loopback local to each container. Published ports install host-to-container forwarding; container-to-container traffic normally uses the destination service's internal port.",
        why: "Hostname/port boundary là root cause phổ biến nhất khi SV1 support Backend, n8n, Keycloak, Qdrant và Ollama.",
        subtopics: ["network namespace", "user-defined bridge", "embedded service DNS", "service name vs hostname", "localhost trap", "internal vs host published port"],
        command: "docker compose config\ndocker compose exec backend getent hosts postgres\ndocker compose exec backend sh -lc 'nc -vz postgres 5432'\ndocker network inspect dxlab_default",
        where: "Host terminal tại Compose project; getent/nc chỉ chạy nếu Backend image có tool đó, nếu không dùng image debug tạm trên cùng network.",
        commandWhy: "Tách DNS resolution, TCP reachability và host publishing thành ba phép đo riêng.",
        expected: "postgres resolve thành container IP; TCP tới postgres:5432 thành công; inspect cho thấy cả service trên cùng network.",
        commandFailure: "Tool không có trong image, service khác network, gọi localhost, dùng host port cho internal call hoặc service chưa listen.",
        guided: "Từ Backend container, resolve và kết nối postgres bằng service name. Từ host, chứng minh postgres service name không phải public DNS và chỉ gọi được khi có host mapping phù hợp.",
        independent: "Vẽ hai đường gọi: Browser→Backend và Backend→Postgres, ghi source namespace, destination hostname, internal/host port cho từng mũi tên.",
        integration: "Đổi DATABASE_URL từ localhost sang postgres và verify bằng DNS, TCP, app health và một API query; nếu query business sai, handoff SV2.",
        failure: "Cố tình đặt Backend và Postgres vào hai network không giao nhau hoặc dùng localhost. Không đổi port ngẫu nhiên; chứng minh DNS/TCP failure trước fix.",
        errors: ["Dùng localhost để gọi container khác", "Dùng host published port giữa containers", "Nghĩ service name dùng được từ browser", "Chỉ ping rồi kết luận app works", "Thay network mà không kiểm callers"],
        selfChecks: ["localhost trong container trỏ đâu?", "Service DNS hoạt động trong scope nào?", "Host port khác internal port ra sao?", "DNS pass có chứng minh DB auth pass không?"],
        evidence: ["terminal-output: DNS + TCP + network membership", "config: URL/hostname matrix redacted", "incident-report: localhost/network failure protocol"],
        requiredEvidence: ["terminal-output", "config", "incident-report"],
        quizConcept: "Containers cùng network gọi nhau bằng service name và internal port; localhost luôn quay lại chính namespace gọi.",
        firstTest: "Từ đúng source container, test service-name resolution rồi TCP tới internal port.",
        shortPrompt: "Backend trong Compose nên dùng hostname nào để gọi service postgres?",
        shortAnswer: "postgres",
        shortExplanation: "Embedded DNS resolve service name postgres trong Compose network.",
        explainPrompt: "Giải thích service DNS, localhost và internal/published port bằng hai đường gọi cụ thể.",
        passExplanation: "namespace, DNS và port forwarding trên mọi mũi tên",
        returnBlock: "Docker DNS/network membership hoặc internal/external port",
      }),
      mission({
        id: "w2-m4-volume-persistence",
        slug: "volume-persistence",
        title: "Volumes, Bind Mounts & Persistence",
        category: "Persistence",
        skillId: "volumes",
        targetLevel: "L4",
        sourceType: official,
        competency: "Named volumes, bind mounts, persistence, recreation, Compose down and destructive down -v warning",
        keywords: ["named volume", "bind mount", "persistence", "recreate", "down", "down -v", "data lifecycle"],
        analogy: "Container là phòng khách sạn; named volume là kho giữ đồ độc lập với phòng; bind mount là mở cửa nối thẳng tới một thư mục của chủ nhà. Trả phòng không mất đồ trong kho, nhưng ra lệnh phá kho thì mất.",
        easy: "Writable layer của container đi theo vòng đời container. Dữ liệu cần sống lâu phải ở named volume hoặc storage được thiết kế. Bind mount tiện cho source/config dev nhưng phụ thuộc path/quyền host. docker compose down không xóa named volume; down -v có thể xóa dữ liệu.",
        technical: "Docker mounts replace a container path with storage managed by a named volume or a host bind source. Container recreation preserves named-volume data when the same volume is reattached. Removing a volume destroys its stored filesystem and is a separate lifecycle operation.",
        why: "SV1 phải chứng minh database/Qdrant/model/progress sống qua restart/recreate và ngăn thao tác down -v phá dữ liệu.",
        subtopics: ["container writable layer", "named volume", "bind mount path/permission", "volume inspect", "restart vs recreate", "docker compose down", "destructive down -v"],
        command: "docker compose exec postgres psql -U dxlab -d dxlab -c \"select 1;\"\ndocker volume ls\ndocker volume inspect dxlab_pgdata\ndocker compose up -d --force-recreate postgres",
        where: "Host terminal tại Compose project và psql bên trong postgres service; dùng tên volume thực từ docker compose config.",
        commandWhy: "Tạo/đọc marker data, inspect storage, recreate container rồi đọc lại marker để chứng minh persistence.",
        expected: "Container ID thay đổi sau recreate nhưng marker row/file trong named volume vẫn tồn tại.",
        commandFailure: "Mount sai target, anonymous volume mới, bind path không tồn tại/quyền sai hoặc vô tình dùng down -v. Không chạy down -v trong lab có dữ liệu thật.",
        guided: "Tạo marker record trong database lab, ghi container ID, force-recreate chỉ Postgres, rồi verify record và volume name. Chụp config trước/sau.",
        independent: "Tự thiết kế persistence test cho một service stateful khác và viết rollback không xóa dữ liệu. Nêu vì sao restart chưa đủ chứng minh recreation persistence.",
        integration: "Trace volume→Postgres→Backend API; verify sau recreate bằng cả row-level query và API smoke, không chỉ volume inspect.",
        failure: "Trong một stack disposable riêng, gắn sai volume target để data biến mất sau recreate. Chẩn đoán mount contract. Chỉ đọc và giải thích down -v warning; không chạy trên learner database.",
        errors: ["Tin container filesystem là backup", "Nhầm restart với recreate", "Dùng bind path cá nhân tuyệt đối", "Chạy down -v theo phản xạ", "Inspect volume nhưng không test business data"],
        selfChecks: ["Container lifecycle khác data lifecycle thế nào?", "Named volume khác bind mount?", "down có xóa volume không?", "Vì sao phải smoke qua app sau recreate?"],
        evidence: ["terminal-output: container ID trước/sau + marker còn tồn tại", "config: named-volume mount contract", "explanation: destructive down -v warning"],
        requiredEvidence: ["terminal-output", "config", "explanation"],
        quizConcept: "Named volume tách data lifecycle khỏi container lifecycle; removing the volume is destructive.",
        firstTest: "So docker compose config/inspect mount source-target và đọc marker data từ service stateful.",
        shortPrompt: "Option nào của docker compose down xóa named volumes?",
        shortAnswer: "-v",
        shortExplanation: "down -v removes named and anonymous volumes declared/attached by the project.",
        explainPrompt: "Giải thích restart, recreate, down và down -v tác động khác nhau lên container/data.",
        passExplanation: "persistence qua recreate và blast radius của volume deletion",
        returnBlock: "mount source/target hoặc data-lifecycle mental model",
      }),
      mission({
        id: "w2-m5-environment-secrets",
        slug: "environment-secrets",
        title: "Environment Contracts, Secrets & Validation",
        category: "Configuration",
        skillId: "environment",
        targetLevel: "L4",
        sourceType: official,
        competency: ".env, .env.example, Compose interpolation, app env, secrets, config validation",
        keywords: [".env", ".env.example", "interpolation", "environment", "secret", "validation", "DATABASE_URL"],
        analogy: ".env.example là danh sách ổ khóa cần loại chìa nào; .env là chùm chìa thật. Compose là người chuyển chìa vào đúng phòng, còn app phải kiểm ngay chìa nào thiếu thay vì chạy nửa vời.",
        easy: "Host .env có thể được Compose dùng để interpolate YAML; environment/env_file chuyển giá trị vào container; app đọc process env. Ba bước này khác nhau. Secret thật không commit, không log, không paste vào evidence. App nên fail fast khi required config thiếu/sai.",
        technical: "Configuration is a versioned contract of required names and semantics. Compose interpolation resolves host-side values before container creation; container environment is runtime process input. Secret material follows the same transport but requires restricted storage, redaction and rotation procedures.",
        why: "Sai hostname/port/issuer/model thường là config drift; SV1 owner env contract và security review, không owner business defaults.",
        subtopics: [".env vs .env.example", "Compose ${VAR} interpolation", "environment vs env_file", "runtime process env", "required/default validation", "secret redaction/rotation", "DATABASE_URL service hostname"],
        command: "docker compose config\ndocker compose run --rm backend sh -lc 'test -n \"$DATABASE_URL\" && echo DATABASE_URL=set'\ngit status --short\ngit check-ignore .env",
        where: "Host terminal tại repository root; runtime env check chạy trong disposable Backend container và chỉ in presence, không in secret value.",
        commandWhy: "Verify host interpolation, runtime presence và repository ignore boundary mà không lộ secret.",
        expected: "Compose config resolve required names; container báo set; git ignore xác nhận .env không tracked; secret value không xuất hiện trong output.",
        commandFailure: "Biến thiếu/typo, empty default, wrong service hostname, shell expansion khác nhau, .env bị stage hoặc debug command in toàn bộ env.",
        guided: "Tạo .env.example chỉ có names/safe placeholders, local .env có lab values, validate compose config và app startup. Thử bỏ một required variable và quan sát fail-fast message không chứa secret.",
        independent: "Viết env matrix name/consumer/required/sensitive/example/validation cho Backend-Postgres; reviewer phải dựng được không hỏi miệng.",
        integration: "Trace DATABASE_URL từ .env→Compose→Backend→postgres DNS. Handoff SV2 nếu connection pass nhưng domain query sai.",
        failure: "Cố tình typo POSTGRES_HOST hoặc để required var rỗng. Thu config/runtime/log evidence, rotate ngay nếu secret vô tình xuất hiện.",
        errors: ["Nghĩ .env.example chứa secret thật", "Nhầm interpolation với runtime env", "In env để debug rồi lộ secret", "Dùng localhost trong DATABASE_URL container", "Dùng silent default che missing config"],
        selfChecks: ["Compose lấy ${VAR} từ đâu?", "env_file khác .env interpolation?", "Làm sao chứng minh var tồn tại mà không in value?", "Secret đã lộ xử lý gì trước?"],
        evidence: ["config: env contract redacted", "terminal-output: config validation + presence + git ignore", "incident-report: missing/typo variable"],
        requiredEvidence: ["config", "terminal-output", "incident-report"],
        quizConcept: "Host interpolation, container environment and application validation are separate links in one configuration chain.",
        firstTest: "Inspect resolved Compose config and validate required variable presence without printing its value.",
        shortPrompt: "File nào nên commit để mô tả required environment names?",
        shortAnswer: ".env.example",
        shortExplanation: ".env.example documents the contract using safe placeholders; real .env stays untracked.",
        explainPrompt: "Giải thích .env → Compose interpolation → container env → app validation và secret boundary.",
        passExplanation: "config chain, redaction và rotation priority",
        returnBlock: "environment contract/interpolation hoặc secret handling",
      }),
      mission({
        id: "w2-m6-health-logs-readiness",
        slug: "health-logs-readiness",
        title: "Healthchecks, Logs & Dependency Readiness",
        category: "Operations",
        skillId: "healthcheck",
        targetLevel: "L4",
        sourceType: extended,
        competency: "Running vs healthy, readiness/liveness, healthcheck timing, pg_isready, /health, service_healthy and retry",
        keywords: ["healthcheck", "readiness", "liveness", "pg_isready", "/health", "service_healthy", "retry", "logs"],
        analogy: "Đèn phòng sáng chỉ nói điện có; bác sĩ đo mạch nói người còn sống; tự đi cầu thang mới chứng minh chức năng. Running, healthy và functional cũng là ba mức khác nhau.",
        easy: "Healthcheck chạy một test lặp lại. interval là khoảng chờ, timeout là thời gian một test, retries là số lần fail, start_period là thời gian khởi động được ưu tiên. DB ready không có nghĩa migration xong; Backend /health pass không có nghĩa mọi business flow pass.",
        technical: "A container healthcheck produces a runtime health state from repeated command exit codes. Readiness asks whether a service can receive dependent traffic; liveness asks whether it should be restarted. Compose service_healthy can delay dependent startup but does not replace application retry/recovery after startup.",
        why: "SV1 phải thiết kế health có ý nghĩa, đọc health logs và phân biệt dependency readiness với business correctness.",
        subtopics: ["running/healthy/functional", "test/interval/timeout/retries/start_period", "CMD vs CMD-SHELL", "pg_isready", "Backend /health", "depends_on condition: service_healthy", "application retry"],
        command: "docker compose ps\ndocker inspect --format '{{json .State.Health}}' dxlab-postgres\ndocker compose exec postgres pg_isready -U dxlab -d dxlab\ncurl -fsS http://localhost:8000/health\ndocker compose logs --tail=100 backend",
        where: "Host terminal tại Compose project; pg_isready chạy trong Postgres service; curl gọi URL host đã publish.",
        commandWhy: "Compare orchestrator state, health history, dependency readiness, app endpoint and logs.",
        expected: "ps/inspect show healthy after start period; pg_isready accepts connection; /health returns success without sensitive details; logs explain transitions.",
        commandFailure: "Health command/tool missing, wrong internal port, too-short start period, endpoint checks only process, DB accepts but schema missing, app lacks retry.",
        guided: "Thêm Postgres pg_isready and Backend /health checks with explicit timing. Measure startup transitions, stop Postgres, observe Backend behavior and recovery after DB returns.",
        independent: "Review one healthcheck: state what it proves, what it cannot prove, false-positive/negative risks, and a functional smoke test that complements it.",
        integration: "Use service_healthy for initial order, then restart Postgres after stack is up to prove Backend retry/recovery rather than startup-only dependency.",
        failure: "Set Backend health target to wrong port or remove start_period. Observe health history and first relevant log; fix timing/target without raising retries blindly.",
        errors: ["Equate running with ready", "Health endpoint exposes config/secret", "Increase retries to hide root cause", "Use host port inside container health", "Assume depends_on handles later outages"],
        selfChecks: ["start_period làm gì?", "pg_isready chứng minh gì và không chứng minh gì?", "Readiness khác liveness?", "Vì sao app vẫn cần retry?"],
        evidence: ["terminal-output: ps/health history/pg_isready/curl", "config: healthcheck fields", "incident-report: wrong target or timing"],
        requiredEvidence: ["terminal-output", "config", "incident-report"],
        quizConcept: "Running, healthy and functional are distinct; health-based startup ordering does not replace application recovery.",
        firstTest: "Inspect health history and run the health command manually in the same container context.",
        shortPrompt: "PostgreSQL readiness command phổ biến là gì?",
        shortAnswer: "pg_isready",
        shortExplanation: "pg_isready checks whether a PostgreSQL server accepts connections.",
        explainPrompt: "Giải thích interval, timeout, retries, start_period và giới hạn của service_healthy.",
        passExplanation: "health semantics, timing và runtime recovery",
        returnBlock: "health command/context/timing hoặc dependency retry",
      }),
      mission({
        id: "w2-m7-http-api-openapi",
        slug: "http-api-openapi",
        title: "HTTP, REST, JSON & OpenAPI Support",
        category: "HTTP/API",
        skillId: "http-rest",
        targetLevel: "L3",
        sourceType: official,
        competency: "Request/response, methods, headers, JSON, status codes, curl and OpenAPI/Swagger",
        keywords: ["http", "rest", "json", "get", "post", "put", "patch", "delete", "status", "curl", "openapi"],
        analogy: "HTTP request giống phiếu yêu cầu có động từ, địa chỉ, headers và body; response là biên nhận có status, headers và nội dung. OpenAPI là mẫu phiếu chính thức để các team không truyền miệng.",
        easy: "GET đọc, POST tạo, PUT thay toàn bộ theo contract, PATCH sửa một phần, DELETE yêu cầu xóa. Status 2xx là accepted/success theo nghĩa endpoint; 4xx thường là request/auth/client contract; 5xx là server-side failure. Luôn đọc response body và correlation ID.",
        technical: "HTTP is a request-response application protocol. REST-style APIs expose resource-oriented representations and semantics through methods, URLs, headers, status codes and JSON bodies. OpenAPI is a machine-readable contract for operations, schemas, parameters, responses and security requirements.",
        why: "SV1 không owner business API nhưng phải test connectivity/contract/auth, đọc Swagger và handoff đúng cho SV2.",
        subtopics: ["request method/path/query/header/body", "JSON content type", "GET/POST/PUT/PATCH/DELETE", "2xx/4xx/5xx", "curl -i/-f/-sS", "OpenAPI operations/schemas/security", "idempotency awareness"],
        command: "curl -i http://localhost:8000/health\ncurl -i -H 'Content-Type: application/json' -d '{\"name\":\"demo\"}' http://localhost:8000/api/items\ncurl -fsS http://localhost:8000/openapi.json",
        where: "Host terminal có curl, gọi published Backend URL; internal callers phải dùng backend:8000 trên Compose network.",
        commandWhy: "Observe status/headers/body and compare actual operation with OpenAPI contract.",
        expected: "Health returns 2xx; create returns documented success/error; OpenAPI JSON lists path/method/schema/security. Exact IDs/timestamps may vary.",
        commandFailure: "Wrong method/path, invalid JSON/Content-Type, 401/403, 404, validation 422/400, proxy route mismatch or app 500.",
        guided: "Chọn một resource trong OpenAPI; test GET, valid POST and invalid POST. Ghi expected status/schema, actual, correlation ID and owner if mismatch.",
        independent: "Không dùng Swagger UI button, tự viết curl cho PATCH/DELETE negative-safe trên disposable record và cleanup record theo API contract.",
        integration: "So Browser request with equivalent curl; if curl passes but browser fails, investigate proxy/CORS/auth context before changing Backend business logic.",
        failure: "Cố tình gửi wrong Content-Type và unknown field. Phân biệt network success với API validation failure; không sửa server để accept arbitrary payload.",
        errors: ["Chỉ nhìn body bỏ status", "Gọi GET để mutate", "Tin 200 nghĩa business data đúng", "Dùng curl host URL bên trong container", "Thay contract miệng không cập nhật OpenAPI"],
        selfChecks: ["401 khác 403?", "PUT khác PATCH?", "HTTP 500 chứng minh transport gì?", "OpenAPI giúp handoff ra sao?"],
        evidence: ["test-result: method/status/schema matrix", "command: reproducible curl commands redacted", "explanation: transport vs API vs business correctness"],
        requiredEvidence: ["test-result", "command", "explanation"],
        quizConcept: "HTTP success at transport/endpoint level must still be checked against the documented API and business expectation.",
        firstTest: "Capture method, URL, status, relevant headers/body and compare the operation with OpenAPI.",
        shortPrompt: "Header nào thường khai báo JSON request body?",
        shortAnswer: "Content-Type: application/json",
        shortExplanation: "Content-Type tells the server how to parse the request representation.",
        explainPrompt: "Phân biệt refused, HTTP 404, validation 4xx, 401, 403 và 500 theo layer/owner.",
        passExplanation: "request/response contract và layer classification",
        returnBlock: "HTTP semantics, curl evidence hoặc OpenAPI contract",
      }),
      mission({
        id: "w2-m8-postgresql-migration-seed",
        slug: "postgresql-migration-seed",
        title: "PostgreSQL, SQL, Migration & Seed Support",
        category: "PostgreSQL",
        skillId: "postgresql",
        targetLevel: "L3",
        sourceType: official,
        competency: "Server/database/schema/table, rows/columns, PK/FK, constraints/indexes, CRUD/JOIN, DATABASE_URL, psql, migration and seed",
        keywords: ["postgresql", "database", "schema", "table", "pk", "fk", "sql", "psql", "migration", "seed", "database_url"],
        analogy: "PostgreSQL server là tòa nhà, database là khu riêng, schema là tầng, table là tủ hồ sơ, row là một phiếu và column là trường. PK là số hồ sơ duy nhất; FK là dây liên kết sang tủ khác.",
        easy: "SV1 cần đủ SQL để chứng minh kết nối/data, không thiết kế business schema thay SV2. Migration version hóa thay đổi schema; seed tạo dữ liệu demo deterministic và phải chạy lại an toàn theo contract.",
        technical: "PostgreSQL organizes databases into schemas and relational tables constrained by keys and rules. SQL SELECT/INSERT/UPDATE/DELETE and JOIN manipulate/query rows. A connection URL carries protocol, credentials, host, port and database. Migrations apply ordered schema changes; seeds establish known reference/demo state.",
        why: "SV1 triage DNS/auth/readiness/migration/seed and operate backup/restore; domain model/business correctness remains SV2 ownership.",
        subtopics: ["server/database/schema/table", "row/column", "PK/FK/constraint/index concept", "SELECT/INSERT/UPDATE/DELETE/JOIN basic", "DATABASE_URL", "pg_isready/psql", "migration status/apply", "idempotent deterministic seed"],
        command: "docker compose exec postgres pg_isready -U dxlab -d dxlab\ndocker compose exec postgres psql -U dxlab -d dxlab -c '\\dt'\ndocker compose exec postgres psql -U dxlab -d dxlab -c 'select count(*) from products;'\ndocker compose exec backend npm run migrate\ndocker compose exec backend npm run seed",
        where: "Host terminal tại Compose project; PostgreSQL commands execute inside postgres; migration/seed commands execute in Backend using repository-defined scripts.",
        commandWhy: "Separate server readiness, authenticated SQL session, schema presence, row evidence and application migration/seed.",
        expected: "pg_isready accepts; psql lists expected tables; count is deterministic after seed; migration reports applied/up-to-date rather than destructive reset.",
        commandFailure: "Wrong DATABASE_URL host/port/password/db, service not ready, missing table/migration, seed duplicates or command names differ from repository.",
        guided: "Connect with psql, identify database/schema/table, run SELECT and one disposable INSERT/UPDATE/DELETE transaction, then rerun deterministic seed and compare counts.",
        independent: "Trace one API request to table rows and a simple JOIN; identify where infra evidence ends and SV2 domain ownership begins.",
        integration: "From Backend container test DNS→TCP→auth→schema→seed→API. Handoff business rule/ERD decisions to SV2 with exact SQL/API evidence.",
        failure: "Use wrong database name then restore it; separately start Backend before migration and diagnose missing relation without resetting the database.",
        errors: ["Dùng localhost in DATABASE_URL", "Nhầm pg_isready với schema ready", "Chạy destructive reset để fix migration", "Seed tạo duplicates", "Sửa domain schema thay SV2"],
        selfChecks: ["PK/FK khác gì?", "pg_isready chứng minh gì?", "Migration khác seed?", "Khi nào handoff SV2?"],
        evidence: ["terminal-output: readiness/psql/table/count", "test-result: migration + idempotent seed rerun", "explanation: infra vs domain boundary"],
        requiredEvidence: ["terminal-output", "test-result", "explanation"],
        quizConcept: "Database readiness, authentication, schema migration, seed state and business correctness are separate checkpoints.",
        firstTest: "Test pg_isready, authenticated psql and migration status in that order before touching domain data.",
        shortPrompt: "Lệnh CLI chuẩn để mở PostgreSQL SQL session là gì?",
        shortAnswer: "psql",
        shortExplanation: "psql is the PostgreSQL interactive terminal/client.",
        explainPrompt: "Giải thích DATABASE_URL và chuỗi DNS→TCP→auth→database→schema→migration→seed→business query.",
        passExplanation: "PostgreSQL connection chain và migration/seed safety",
        returnBlock: "DB connection, schema migration hoặc deterministic seed",
      }),
      mission({
        id: "w2-m9-reverse-proxy-integration",
        slug: "reverse-proxy-integration",
        title: "Reverse Proxy & Multi-layer API Integration",
        category: "Reverse Proxy",
        skillId: "reverse-proxy",
        targetLevel: "L3",
        sourceType: extended,
        competency: "Reverse proxy purpose, upstream, path routing, 502/504, internal/external URLs and multi-layer incident triage",
        keywords: ["reverse proxy", "upstream", "path routing", "502", "504", "internal url", "external url", "integration"],
        analogy: "Reverse proxy là lễ tân: khách chỉ biết quầy trước, lễ tân chuyển /api tới Backend đúng phòng. 502 là lễ tân không nhận được trả lời hợp lệ từ phòng; 504 là chờ phòng quá lâu.",
        easy: "Browser calls external URL; proxy calls internal upstream like backend:8000. Path rewrite and forwarded headers must match app contract. Proxy reachability does not replace Backend health, DB readiness or migration checks.",
        technical: "A reverse proxy terminates a client-facing connection and creates an upstream request according to routing rules. Gateway 502 commonly signals invalid/unreachable upstream response; 504 signals upstream timeout. Internal service URLs and public browser URLs inhabit different name/port scopes.",
        why: "SV1 owner proxy/network integration and must localize failure without becoming deep proxy administrator or changing SV2 business code.",
        subtopics: ["proxy purpose", "upstream host/port", "path prefix/rewrite", "forwarded host/proto awareness", "502 vs 504", "internal vs external URL", "layer-by-layer test order"],
        command: "curl -i http://localhost/api/health\ndocker compose exec proxy sh -lc 'wget -qO- http://backend:8000/health'\ndocker compose logs --tail=100 proxy\ndocker compose logs --tail=100 backend\ndocker compose config",
        where: "First curl from host to external proxy URL; upstream test from proxy network context; logs/config from Compose project.",
        commandWhy: "Compare outside-in request with direct internal upstream and correlated logs.",
        expected: "External and internal health return compatible success; proxy logs show routed path/upstream; Backend logs show matching request.",
        commandFailure: "Wrong service DNS/port, path rewrite mismatch, Backend unhealthy, timeout, proxy container lacks test tool or browser uses internal URL.",
        guided: "Configure /api path to backend:8000, test direct upstream then external route. Record request path/status at proxy and Backend.",
        independent: "Given a 502, produce a test tree covering proxy process/listen, DNS, TCP, Backend health, port, path and logs; execute one hypothesis at a time.",
        integration: "Run Portal/API request through proxy, then equivalent curl direct upstream. Add DB/migration checks only after upstream reaches Backend and logs show DB failure.",
        failure: "Point upstream to backend:8001 or a wrong service name. Diagnose 502 without changing public host port; then create controlled slow upstream to recognize 504 semantics.",
        errors: ["Give Browser backend:8000 internal URL", "Treat every 502 as proxy syntax", "Increase timeout before evidence", "Rewrite path twice", "Change ports across services simultaneously"],
        selfChecks: ["Proxy upstream URL dùng scope nào?", "502 khác 504?", "Direct upstream pass nhưng browser fail gợi ý gì?", "Khi nào kiểm DB?"],
        evidence: ["terminal-output: external vs internal curl and relevant logs", "config: upstream/path route", "incident-report: hidden multi-layer root cause"],
        requiredEvidence: ["terminal-output", "config", "incident-report"],
        quizConcept: "Reverse proxy and upstream are separate hops; test external route and internal upstream to localize 502/504.",
        firstTest: "Call the upstream from proxy network context and compare with the external URL plus both service logs.",
        shortPrompt: "HTTP status nào thường chỉ proxy không nhận response hợp lệ từ upstream?",
        shortAnswer: "502",
        shortExplanation: "502 Bad Gateway indicates a gateway/proxy received an invalid or unavailable upstream response.",
        explainPrompt: "Giải thích Browser→proxy→Backend→Postgres bằng hostname/port/network/status và test order.",
        passExplanation: "external/internal URL, upstream routing và multi-layer isolation",
        returnBlock: "proxy upstream/path contract hoặc downstream Backend/DB layer",
      }),
    ],
  },
  ...laterWeeks,
];
