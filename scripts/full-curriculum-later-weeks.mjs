import { weeksFourToEight } from "./full-curriculum-weeks-4-8.mjs";

const official = "THEO_KE_HOACH_CO";

const base = {
  estimatedMinutes: 210,
  quizPassScore: 75,
  objectives: [
    "Giải thích concept bằng analogy và technical language",
    "Tự chạy lab và phân biệt expected/actual",
    "Debug failure bằng evidence protocol",
  ],
  cleanup:
    "khôi phục exact config/export đã sao lưu trong lab, xác nhận use case cũ vẫn pass và không xóa persistent storage",
  regression:
    "chạy lại health, negative test và một caller/use case lân cận sau khi sửa",
};

function mission(value) {
  return { ...base, ...value };
}

export const laterWeeks = [
  {
    week: 3,
    previousMissionId: "w2-m9-reverse-proxy-integration",
    missions: [
      mission({
        id: "w3-m1-identity-authentication-authorization",
        slug: "identity-authentication-authorization",
        title: "Identity: Authentication, Authorization & OIDC Map",
        category: "Identity",
        skillId: "authentication",
        targetLevel: "L4",
        sourceType: official,
        competency: "Authentication versus authorization, OAuth2 enough-to-use, OIDC, browser-to-Backend identity flow",
        keywords: ["identity", "authentication", "authorization", "oauth2", "oidc", "browser", "backend"],
        analogy: "Authentication giống kiểm tra bạn là ai ở cổng; authorization giống kiểm thẻ đó được vào phòng nào. Có thẻ nhân viên không có nghĩa được vào két sắt. OAuth2/OIDC là quy trình cấp và mang thẻ giữa các hệ thống.",
        easy: "Login trả lời 'ai đang dùng hệ thống'. Role/permission trả lời 'người đó được làm gì'. OIDC bổ sung identity information trên nền OAuth2 để application biết user; Backend phải tự verify token và enforce permission, không tin UI ẩn nút.",
        technical: "Authentication establishes a subject identity. Authorization evaluates whether that subject may perform an action on a resource. OAuth 2.0 delegates authorization; OpenID Connect layers identity tokens and standardized claims over OAuth 2.0. A resource server validates access tokens and makes its own policy decision.",
        why: "SV1 thiết kế/điều phối identity boundary, URL, client config, token validation and failure triage; SV2 owns business authorization rules and SV3 owns Portal implementation.",
        subtopics: ["identity provider/client/resource server", "authentication vs authorization", "OAuth2 roles at a useful level", "OIDC identity claims", "browser/portal/Keycloak/backend flow", "UI visibility vs Backend enforcement"],
        command: "curl -fsS http://localhost:8080/realms/dx-lab/.well-known/openid-configuration\ncurl -i http://localhost:8000/api/protected\ndocker compose ps keycloak backend\ndocker compose logs --tail=100 keycloak",
        where: "Host terminal at the Compose project. URLs/realm/ports are lab examples; use the frozen project contract instead of inventing production values.",
        commandWhy: "Observe discovery metadata, an unauthenticated resource response, service state and identity logs without printing user tokens.",
        expected: "Discovery returns issuer/endpoints/JWKS URI; protected API returns documented 401 without a token; Keycloak and Backend have observable state/logs.",
        commandFailure: "Wrong realm/port, IdP not ready, browser URL confused with internal hostname, API has no guard, or a raw token is accidentally copied into output.",
        guided: "Draw User → Portal → Keycloak → Authorization Code → token → Backend. Mark which component authenticates, validates, authorizes and stores no token. Verify discovery and a no-token 401.",
        independent: "Given 'the button is hidden but curl still succeeds', explain why it is an authorization defect and write a safe Backend-focused handoff to SV2.",
        integration: "Compare browser-facing issuer URL with Backend issuer/JWKS reachability. Test an unauthenticated API request and keep any real bearer token out of screenshots/evidence.",
        failure: "Point the Backend issuer setting to a wrong realm in a disposable config. Capture discovery/Backend evidence, classify issuer versus network failure, then restore it.",
        errors: ["Call a hidden UI element security", "Confuse OAuth2 and OIDC as identical", "Log raw access tokens", "Make Keycloak the only enforcement point", "Use internal Keycloak hostname in browser"],
        selfChecks: ["Authentication khác authorization?", "OIDC thêm gì vào OAuth2 use case?", "Ai enforces protected API?", "401 không token nói layer nào đang hoạt động?"],
        evidence: ["terminal-output: discovery + no-token 401 without secrets", "explanation: identity flow and Backend boundary", "incident-report: wrong issuer/realm diagnosis"],
        requiredEvidence: ["terminal-output", "explanation", "incident-report"],
        quizConcept: "Authentication proves identity; authorization is an independent Backend policy decision for a specific action/resource.",
        firstTest: "Fetch OIDC discovery and make an unauthenticated protected-resource request before changing any policy.",
        shortPrompt: "Protocol layer nào chuẩn hóa identity claims trên OAuth 2.0?",
        shortAnswer: "OIDC",
        shortExplanation: "OpenID Connect is the identity layer built on OAuth 2.0.",
        explainPrompt: "Giải thích User→Portal→Keycloak→token→Backend và vì sao hidden button không phải security control.",
        passExplanation: "identity versus permission boundary and token-validation ownership",
        returnBlock: "identity-flow mental model hoặc authentication/authorization distinction",
      }),
      mission({
        id: "w3-m2-keycloak-realm-client-users-roles",
        slug: "keycloak-realm-client-users-roles",
        title: "Keycloak Realm, Client, Users & Roles",
        category: "Keycloak",
        skillId: "keycloak",
        targetLevel: "L4",
        sourceType: official,
        competency: "Keycloak realm/client/users/roles model and four-role DX-Lab demo setup",
        keywords: ["keycloak", "realm", "client", "user", "role", "admin", "manager", "sales", "warehouse"],
        analogy: "Realm là khuôn viên trường; client là một tòa nhà đăng ký; users là người có thẻ; roles là quyền chức năng được cấp. Gắn role vào user không tự khiến mọi tòa nhà hiểu role nếu mapper/client contract chưa đúng.",
        easy: "Tạo realm riêng dx-lab, client cho Portal, demo users and roles admin/manager/sales/warehouse. Credentials demo là dữ liệu lab, khác secret cá nhân. Chỉ redirect URI thật sự cần thiết; wildcard rộng làm tăng risk.",
        technical: "A Keycloak realm isolates users, credentials, clients, roles and protocol configuration. A client defines an application integration and allowed redirect URIs. Realm/client roles are assigned to users or groups and emitted only according to protocol mappers/scopes.",
        why: "SV1 owns identity configuration/reproducibility and security coordination. The team requires a stable role matrix for Backend, workflow and demo, not manually assigned ad hoc access.",
        subtopics: ["realm isolation", "confidential/public client awareness", "client ID", "valid redirect URI", "web origins awareness", "realm/client roles", "demo users/password policy", "role assignment"],
        command: "docker compose exec keycloak /opt/keycloak/bin/kc.sh show-config\ncurl -fsS http://localhost:8080/realms/dx-lab/.well-known/openid-configuration\ndocker compose logs --tail=100 keycloak\ndocker compose config",
        where: "Host terminal at Compose root; `kc.sh show-config` runs inside the Keycloak container. Manage realms in the admin UI or versioned import file, never by pasting admin credentials into evidence.",
        commandWhy: "Confirm runtime configuration and realm discovery while preserving credential boundaries.",
        expected: "Keycloak process and realm discovery are available; versioned realm configuration defines Portal client, four demo roles/users and constrained redirect URL.",
        commandFailure: "Wrong admin bootstrap env, client uses wrong redirect URL, duplicate role spelling, realm config not persisted/exported, or wildcard redirect set too wide.",
        guided: "In a disposable dx-lab realm, create the Portal client, four roles and four demo users. Build a matrix user→role→allowed scenario. Verify discovery and logout/login with one demo account without recording passwords.",
        independent: "Add a hypothetical Accountant role only as an impact analysis: list Keycloak, claim mapper, Backend RBAC, workflow, Portal awareness, tests, demo docs. Do not silently implement other owners' modules.",
        integration: "Map role owner expectations across Backend and workflow. Ask SV2 for accepted authorization policy; then verify Keycloak emits intended role inputs, not business permissions.",
        failure: "Configure a redirect URI that differs by port/path from Portal callback. Capture client config/browser symptom/IdP log and fix only the exact URI; do not use wildcard as a shortcut.",
        errors: ["Use Keycloak master realm for app users", "Share real password as demo password", "Wide wildcard redirect", "Assume role assignment alone proves token claim", "Mix admin client and Portal client"],
        selfChecks: ["Realm isolates which objects?", "Client config controls what browser redirect risk?", "Why keep demo credentials distinct?", "New role change hits which owners?"],
        evidence: ["config: redacted realm/client/role matrix", "terminal-output: discovery/runtime health", "explanation: role impact analysis"],
        requiredEvidence: ["config", "terminal-output", "explanation"],
        quizConcept: "A Keycloak realm owns isolated identity objects; a client defines one application's protocol/redirect contract.",
        firstTest: "Verify realm discovery and compare exact Portal callback URL to the configured valid redirect URI.",
        shortPrompt: "Keycloak object nào tách users, clients and roles của application?",
        shortAnswer: "realm",
        shortExplanation: "A realm is the tenant/security boundary for Keycloak-managed identity objects.",
        explainPrompt: "Giải thích realm/client/user/role and the safe four-demo-role setup without exposing credentials.",
        passExplanation: "realm/client contract, role matrix and redirect security",
        returnBlock: "realm/client/role model or exact redirect configuration",
      }),
      mission({
        id: "w3-m3-authorization-code-pkce-sso",
        slug: "authorization-code-pkce-sso",
        title: "Authorization Code + PKCE & SSO",
        category: "OIDC Flow",
        skillId: "oidc",
        targetLevel: "L4",
        sourceType: official,
        competency: "Authorization Code flow, PKCE, state/nonce concepts, callback, SSO and browser integration awareness",
        keywords: ["authorization code", "pkce", "code verifier", "code challenge", "state", "nonce", "callback", "sso"],
        analogy: "Authorization code giống phiếu gửi xe chỉ dùng được một lần. PKCE thêm dấu niêm phong do Portal tạo: chỉ Portal giữ verifier mới đổi được phiếu lấy token. SSO là dùng một lần xác minh ở cổng chung để vào nhiều tòa nhà đã tin cậy.",
        easy: "Browser được redirect đến Keycloak, login, rồi quay về exact callback với code/state. Portal đổi code cùng verifier lấy token. Code không phải token và không được log. state giúp liên kết request/callback; nonce liên quan protection for ID token replay. PKCE is standard for browser/public clients.",
        technical: "Authorization Code with PKCE binds an authorization request's code_challenge to a token-exchange code_verifier, reducing authorization-code interception risk for public clients. The client validates state correlation and OIDC nonce semantics; an IdP session can supply SSO across participating clients.",
        why: "SV1 must debug redirect/callback/session behavior and audit URL/config risk while leaving Portal code at L0 boundary.",
        subtopics: ["authorization endpoint/token endpoint", "redirect URI callback", "authorization code single use", "PKCE verifier/challenge", "state", "nonce", "IdP session and SSO", "logout/session boundary"],
        command: "curl -fsS http://localhost:8080/realms/dx-lab/.well-known/openid-configuration\ndocker compose logs --tail=100 keycloak\ndocker compose logs --tail=100 portal\ndocker compose config",
        where: "Use browser devtools only to observe redirect URLs safely; run curl/log/config from host. Never copy authorization codes, ID tokens or access tokens into Evidence Vault.",
        commandWhy: "Discovery exposes protocol endpoints; logs/config localize callback and session failures without leaking bearer material.",
        expected: "Browser redirects to configured authorization endpoint and returns only to exact callback; SSO may skip repeated login when IdP session is valid.",
        commandFailure: "Callback mismatch, HTTP versus HTTPS mismatch, stale state/cookie, bad client ID, PKCE disabled/misconfigured, or cookies blocked by browser policy.",
        guided: "Map one login trace with names only: initial URL, authorization endpoint, callback, token exchange owner and protected API call. Test login then a second client or renewed navigation to recognize SSO session behavior.",
        independent: "Explain why a public Portal should not embed a client secret and why a code in browser URL must not become an evidence artifact.",
        integration: "Coordinate Portal callback config with Keycloak client exact URI. Verify Backend receives access token only after Portal's code exchange; if UI state is faulty but identity flow is valid, handoff SV3.",
        failure: "Change callback port/path in a disposable client setting. Capture redirect error, discovery and client config; restore exact URI rather than broadening allowed patterns.",
        errors: ["Treat code as bearer token", "Put client secret in browser", "Disable PKCE to make test pass", "Ignore state mismatch", "Assume SSO means API authorization passes"],
        selfChecks: ["Why PKCE?", "Code khác token?", "state and nonce protect what broadly?", "SSO affects which layer?"],
        evidence: ["explanation: redacted flow diagram", "config: exact callback/PKCE policy", "incident-report: callback/state failure"],
        requiredEvidence: ["explanation", "config", "incident-report"],
        quizConcept: "Authorization Code + PKCE exchanges a short-lived code only when the requester proves possession of the original verifier.",
        firstTest: "Compare the browser callback URL exactly with Keycloak client redirect configuration and inspect the first redirect error.",
        shortPrompt: "PKCE proof value kept by the client is called what?",
        shortAnswer: "code verifier",
        shortExplanation: "The code verifier is held by the client and used at token exchange to prove the original request binding.",
        explainPrompt: "Giải thích Authorization Code + PKCE from browser redirect to Backend API and name each secret/non-secret boundary.",
        passExplanation: "code/callback/PKCE lifecycle and SSO scope",
        returnBlock: "OIDC redirect/PKCE flow or browser session boundary",
      }),
      mission({
        id: "w3-m4-jwt-jwks-validation",
        slug: "jwt-jwks-validation",
        title: "JWT Claims, JWKS & Backend Validation",
        category: "JWT",
        skillId: "jwt",
        targetLevel: "L4",
        sourceType: official,
        competency: "JWT header/payload/signature, sub/iss/aud/azp/exp/roles, JWKS and decode versus validate",
        keywords: ["jwt", "jwks", "signature", "issuer", "audience", "azp", "exp", "claims", "validation"],
        analogy: "JWT payload là phần chữ nhìn thấy trên thẻ; signature là con dấu chống sửa. Đọc chữ trên thẻ không chứng minh con dấu thật, thẻ do đúng trường cấp, dành đúng cửa hoặc còn hạn.",
        easy: "JWT thường có header.payload.signature. Decode chỉ xem payload base64url; validation must verify signature with trusted JWKS key, issuer, audience/client expectations, expiry and policy. Backend never accepts a token just because a browser decoded it.",
        technical: "A JSON Web Token is a compact signed claims set. Secure resource-server validation resolves trusted issuer metadata/JWKS, verifies the cryptographic signature and validates claims such as issuer, audience/authorized party, expiration/not-before and authorization claims before use.",
        why: "SV1 must localize 401 to token/header/issuer/audience/JWKS/clock/config layers and coordinate safe evidence without becoming cryptography implementer.",
        subtopics: ["JWS structure", "sub/iss/aud/azp/exp roles", "JWKS key discovery and rotation", "decode vs cryptographic validation", "Bearer header", "clock skew", "sanitized claim evidence"],
        command: "curl -fsS http://localhost:8080/realms/dx-lab/.well-known/openid-configuration\ncurl -fsS http://localhost:8080/realms/dx-lab/protocol/openid-connect/certs\ncurl -i -H 'Authorization: Bearer REDACTED_TOKEN' http://localhost:8000/api/protected\ndocker compose logs --tail=100 backend",
        where: "Run discovery/JWKS from host. For protected endpoint, use a deliberately redacted placeholder in documentation; in a real lab, do not store/copy the full bearer token into terminal history or evidence.",
        commandWhy: "Check issuer-discovery/JWKS reachability and observe documented 401 behavior plus Backend validation logs.",
        expected: "Discovery declares issuer/JWKS URI; JWKS returns public keys; missing/invalid/redacted bearer yields 401 without token echo in logs.",
        commandFailure: "Wrong issuer URL, stale JWKS/cache, invalid signature, expired token, audience mismatch, missing Authorization prefix, server clock skew or raw token logging.",
        guided: "Create a sanitized claim worksheet with labels sub/iss/aud/azp/exp/roles from a demo token viewed locally, then validate which checks Backend—not frontend—must enforce. Do not submit token string.",
        independent: "Write a 401 triage order: Authorization header → token existence → exp → iss → aud/azp → signature/JWKS → clock → Backend logs. Explain why decode does not replace it.",
        integration: "Verify Keycloak discovery reachable from Backend network and public browser URL use correct route. Handoff to SV2 only after cryptographic/config checks pass and endpoint policy behavior remains wrong.",
        failure: "Set expected issuer with a trailing realm mismatch or use an expired test token. Gather 401, issuer/JWKS/config/log evidence; fix issuer/refresh token pathway without bypassing validation.",
        errors: ["Decode and call it validate", "Trust alg/header from attacker", "Disable issuer check", "Log tokens", "Treat all 401 as role issue"],
        selfChecks: ["Decode proves what?", "JWKS holds what?", "iss/aud/azp differences?", "Which checks precede role debugging?"],
        evidence: ["terminal-output: discovery/JWKS/401 status without secret", "explanation: claim validation checklist", "incident-report: issuer/audience/expiry 401"],
        requiredEvidence: ["terminal-output", "explanation", "incident-report"],
        quizConcept: "JWT decode reveals untrusted data; validation verifies signature and contextual claims against a trusted issuer/JWKS policy.",
        firstTest: "Classify the response as missing/invalid token 401, then trace header→exp→issuer→audience→JWKS→clock.",
        shortPrompt: "Public signing keys used to verify JWT signatures are normally published through what document?",
        shortAnswer: "JWKS",
        shortExplanation: "A JSON Web Key Set publishes public verification keys associated with an issuer.",
        explainPrompt: "Giải thích why decode ≠ validate and walk through iss/aud/azp/exp/JWKS checks for a 401.",
        passExplanation: "JWT trust chain and safe 401 triage",
        returnBlock: "token validation claim/JWKS/clock chain",
      }),
      mission({
        id: "w3-m5-rbac-roles-claims-backend-guard",
        slug: "rbac-roles-claims-backend-guard",
        title: "RBAC: Roles, Claims, Mappers & Backend Guard",
        category: "Authorization",
        skillId: "rbac",
        targetLevel: "L4",
        sourceType: official,
        competency: "Role claims, mapper, user assignment, Backend guard and 401/403/200 access matrix",
        keywords: ["rbac", "role", "claim", "mapper", "backend guard", "401", "403", "200", "matrix"],
        analogy: "Keycloak role assignment là cấp thẻ màu; mapper là máy in đưa màu lên thẻ token; Backend guard là nhân viên cửa kiểm màu cho từng phòng. Nếu máy in bỏ màu, người có quyền vẫn bị từ chối.",
        easy: "401 means no valid identity/token; 403 means token valid but policy denies action. For 403, verify expected role assignment, token claim shape/location, mapper/scope and Backend guard. Never fix by making all users admin or trusting a frontend role flag.",
        technical: "Role-based access control maps authenticated subjects to role claims and evaluates endpoint/resource policy. IdP role assignment must be emitted through correct mapper/scope contract, then parsed by Backend policy. A robust matrix tests allowed and denied cases independently of UI.",
        why: "SV1 coordinates identity and integration proof. SV2 owns policy semantics, but a reproducible 401/403/200 matrix prevents handoff ambiguity and unsafe demo shortcuts.",
        subtopics: ["realm/client role source", "protocol mapper/scope", "claim path", "user/group assignment", "Backend guard", "401 versus 403", "positive/negative access matrix", "least privilege"],
        command: "curl -i http://localhost:8000/api/manager-only\ncurl -i -H 'Authorization: Bearer REDACTED_MANAGER_TOKEN' http://localhost:8000/api/manager-only\ndocker compose logs --tail=100 backend\ndocker compose logs --tail=100 keycloak",
        where: "Host terminal at Compose root; use local secure test tooling for real tokens and record only status/claim labels, never bearer strings.",
        commandWhy: "Produce no-token/allowed/denied response evidence and correlate IdP/Backend logs.",
        expected: "No token returns 401; correct valid role returns documented success; valid wrong role returns 403; logs identify policy without exposing token payload.",
        commandFailure: "Role absent in token, mapper wrong scope/path, client role vs realm role confusion, user assignment stale until fresh token, Backend reads wrong claim or guard missing.",
        guided: "Create a 4×3 matrix for admin/manager/sales/warehouse against three protected actions. Run at least one 401, one allowed 200/2xx and one denied 403 per policy class.",
        independent: "Given valid token + 403, write evidence-first triage and propose a minimal mapper/guard contract change without expanding privilege.",
        integration: "Align Keycloak claim path with Backend parser and workflow approval actor. Handoff SV2 only with token claim labels, exact endpoint, expected/actual status and no token body.",
        failure: "Remove/alter the role mapper in a lab client or assign wrong role. Refresh token, show 403, trace mapper/claim/guard and restore precise mapping.",
        errors: ["Call 403 authentication failure", "Use old token after role change", "Make frontend decide authorization", "Grant admin to debug", "Test only happy path"],
        selfChecks: ["401 vs 403?", "Why refresh token after role assignment?", "Mapper failure shows where?", "Which component actually denies endpoint?"],
        evidence: ["test-result: redacted 401/403/2xx RBAC matrix", "explanation: role→claim→guard chain", "incident-report: mapper/guard 403 root cause"],
        requiredEvidence: ["test-result", "explanation", "incident-report"],
        quizConcept: "A 403 follows successful token validation but failed authorization; triage role assignment, mapper/claim and Backend guard in order.",
        firstTest: "Verify valid-token status separately from role claim and Backend guard, using a fresh token after role changes.",
        shortPrompt: "Status code for valid identity but insufficient permission is what?",
        shortAnswer: "403",
        shortExplanation: "403 Forbidden indicates the server understood/authenticated the request but refuses authorization.",
        explainPrompt: "Explain user role assignment → token claim mapper → Backend guard and why each positive/negative matrix row matters.",
        passExplanation: "least-privilege role claim to Backend enforcement chain",
        returnBlock: "role assignment/mapper claim or Backend policy contract",
      }),
      mission({
        id: "w3-m6-identity-debug-401-403-expiry",
        slug: "identity-debug-401-403-expiry",
        title: "Identity Incident Triage: 401, 403 & Expiry",
        category: "Troubleshooting",
        skillId: "troubleshooting",
        targetLevel: "L4",
        sourceType: official,
        competency: "Evidence-first 401/403/expiry/issuer/audience/role troubleshooting protocol",
        keywords: ["401", "403", "expiry", "issuer", "audience", "role", "debug", "evidence", "clock"],
        analogy: "401 giống thẻ không hợp lệ/hết hạn tại cổng chính; 403 giống thẻ thật nhưng không mở đúng phòng. Kiểm nhầm danh sách phòng khi thẻ đã hết hạn chỉ lãng phí thời gian.",
        easy: "Start with symptom and status. 401 path is Authorization header→token→exp→iss→aud/azp→signature/JWKS→clock. 403 path starts only after valid token then role claim→mapper→assignment→Backend guard. Do not restart every service or disable checks.",
        technical: "Incident triage follows protocol-driven discriminating tests. Status code, sanitized headers, issuer metadata, verification logs, token timing and role/claim observations distinguish authentication validation defects from authorization policy defects and configuration drift.",
        why: "Identity failures often block all team modules. SV1 needs a repeatable handoff artifact with safe evidence, likely layer/owner and verification/regression, not a vague screenshot.",
        subtopics: ["symptom classification", "safe evidence redaction", "401 decision tree", "403 decision tree", "token expiry/clock", "issuer/audience/JWKS", "mapper/guard", "handoff format"],
        command: "curl -i http://localhost:8000/api/protected\ncurl -fsS http://localhost:8080/realms/dx-lab/.well-known/openid-configuration\ncurl -fsS http://localhost:8080/realms/dx-lab/protocol/openid-connect/certs\ndocker compose logs --tail=100 backend\ndocker compose logs --tail=100 keycloak",
        where: "Host terminal at Compose root. Use redacted tokens only in written commands; inspect real claims locally with approved tooling and capture labels/status, not bearer material.",
        commandWhy: "Collect independent evidence for missing token, issuer/JWKS reachability, Backend validation and IdP event context.",
        expected: "Each path leads to a concrete evidence-backed hypothesis rather than a random configuration change; logs avoid raw token disclosure.",
        commandFailure: "Mixing 401/403 paths, testing token with wrong environment, log truncation hiding first error, stale token after config change, or leaking token through shell history.",
        guided: "Run three controlled cases: no token (401), valid wrong role (403), expired token (401). Fill all nine protocol fields and write one line explaining why each test distinguishes a hypothesis.",
        independent: "Take the Week 3 Boss Fight only after the guided matrix. If assisted, it remains learning evidence but cannot count as clean PASS.",
        integration: "Create a handoff for SV2 or SV3 containing endpoint, expected/actual status, issuer/claim checks, reproduction, likely layer and correlation/time. Do not prescribe changes outside SV1 scope without evidence.",
        failure: "Inject wrong issuer or expired test token, then separately remove a role mapper. Keep cases isolated; show why one is 401 and the other 403.",
        errors: ["Use role checklist for 401", "Use JWKS checklist for pure 403", "Paste token into Issue", "Disable validation", "Forget refreshed token"],
        selfChecks: ["First three 401 checks?", "First three 403 checks?", "Why not restart before logs?", "What is a safe handoff payload?"],
        evidence: ["incident-report: all nine protocol fields", "terminal-output: sanitized discovery/JWKS/log status", "explanation: 401/403 branch decision"],
        requiredEvidence: ["incident-report", "terminal-output", "explanation"],
        quizConcept: "401 and 403 require different evidence trees; the status code is an early discriminating signal, not merely an error label.",
        firstTest: "Record status and safe request context, then follow the matching 401 or 403 decision tree without changing config.",
        shortPrompt: "Claim nào commonly indicates token expiration time?",
        shortAnswer: "exp",
        shortExplanation: "The exp claim represents the expiration time after which a token must not be accepted.",
        explainPrompt: "Walk through a 401 and a 403 triage in parallel, naming the first evidence and test at each branch.",
        passExplanation: "status-specific evidence protocol and safe escalation",
        returnBlock: "401/403 classification or identity test ordering",
      }),
      mission({
        id: "w3-m7-realm-export-security",
        slug: "realm-export-security",
        title: "Realm Export/Import, Security Review & Reproducibility",
        category: "Identity Operations",
        skillId: "security-review",
        targetLevel: "L4",
        sourceType: official,
        competency: "Keycloak realm export/import, persistence, secret protection, redirect review and reproducible identity setup",
        keywords: ["realm export", "realm import", "persistence", "redirect uri", "secrets", "identity", "reproducibility", "security"],
        analogy: "Realm export giống bản vẽ có thể dựng lại trường học, không phải ảnh chụp ký túc xá đang chứa chìa khóa thật. Cần biết bản vẽ có gì, thiếu gì, ai giữ và test dựng lại trước khi tin nó.",
        easy: "Version/export identity configuration enough to recreate realm/client/roles/users policy without storing real admin secrets. Keycloak persistence and import strategy must be tested on a clean/disposable target. Review redirect URLs, web origins, demo users and token/log exposure.",
        technical: "Realm export/import serializes selected identity configuration for reproducibility. Safe handling separates non-secret configuration from bootstrap/admin credentials. A recovery drill imports into a distinct target/persistent volume and verifies discovery, login, token validation and RBAC matrix.",
        why: "Week 3 gate includes reproducibility and security closure. SV1 coordinates backup/export, verifies persistence and documents source of truth; no identity state should be recreated by undocumented clicks.",
        subtopics: ["realm export scope", "import target and overwrite risk", "Keycloak database/volume persistence", "bootstrap admin secret boundary", "redirect/web-origin review", "demo credential policy", "recovery drill", "identity runbook"],
        command: "docker compose ps keycloak\ndocker compose config\ndocker volume inspect dxlab_keycloak_data\ncurl -fsS http://localhost:8080/realms/dx-lab/.well-known/openid-configuration\ndocker compose logs --tail=100 keycloak",
        where: "Host terminal in Compose project. Export/import commands depend on selected Keycloak version and project runbook; operate only against a disposable realm/volume during the drill.",
        commandWhy: "Verify storage/config/discovery/log evidence around a controlled export/import without exposing bootstrap secrets.",
        expected: "A fresh/disposable target can recreate documented realm/client/roles and pass discovery plus RBAC smoke; persistent storage remains separate from container lifecycle.",
        commandFailure: "Import into active production realm, export excludes expected objects, secret values appear in file, volume not persisted, redirect/web origin drift or version incompatibility.",
        guided: "Create an export manifest listing realm, client IDs, roles, users policy, mappers, redirect rules and excluded secrets. Import into a disposable target, then run discovery + 401/403/2xx smoke.",
        independent: "Write an identity recovery checklist that distinguishes export from backup, identifies owner/access control, and says how to prove old real secret is not in the artifact.",
        integration: "Verify Portal client, Backend issuer/JWKS and workflow service account conventions against imported realm. Handoff functional Portal code issues to SV3 after identity contract passes.",
        failure: "Attempt an import with a missing mapper or wrong redirect in a disposable target. Detect it with the role/callback matrix, correct export/source configuration and re-run full smoke.",
        errors: ["Treat export as complete DB backup", "Import over active realm", "Commit admin secret", "Assume volume exists without recreate test", "Document redirect wildcard as convenient"],
        selfChecks: ["Export differs from backup?", "What must never enter versioned realm config?", "How do you prove import reproducibility?", "Which tests after import?"],
        evidence: ["config: export manifest/redacted source-of-truth map", "test-result: separate-target import smoke", "explanation: identity security/recovery boundary"],
        requiredEvidence: ["config", "test-result", "explanation"],
        quizConcept: "Realm export makes identity configuration reproducible only when paired with secret separation, a distinct-target import drill and functional RBAC verification.",
        firstTest: "Use a disposable target, import documented configuration, then verify discovery, callback, 401/403/2xx and persistence before calling it reproducible.",
        shortPrompt: "Which action is unsafe for a recovery drill on an active learner/production realm?",
        shortAnswer: "overwrite import",
        shortExplanation: "An overwrite import can destroy active identity configuration and must be restricted to explicit, protected recovery operations.",
        explainPrompt: "Explain realm export/import, persistence, secret separation and the exact smoke proof needed after recovery.",
        passExplanation: "reproducible identity configuration and security closure",
        returnBlock: "export/import safety, persistence or identity smoke matrix",
      }),
    ],
  },
  ...weeksFourToEight,
];
